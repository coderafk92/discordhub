import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Discord Hub - Trade, Connect, Community",
  description: "The ultimate Discord hub for trading, connecting with friends, and exploring servers",
  openGraph: {
    title: "Discord Hub",
    description: "Trade OWO, Nitro, game items, and more with verified traders",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable}`}
      suppressHydrationWarning
    >
      <body className="bg-gray-900 text-white">{children}</body>
    </html>
  );
}
